# =============================================================================
# SAMPLE ENVIRONMENT CONFIGURATION
# =============================================================================
# Copy this file to .env and update the values according to your setup
# NEVER commit the actual .env file to version control!

#--- Django Secret Key ---
# Generate a new secret key for production: python -c "import secrets; print(secrets.token_urlsafe(50))"
SECRET_KEY=your-secret-key-here

# --- PostgreSQL Database Settings ---
POSTGRES_DB=sports_db  
POSTGRES_USER=sports_user
POSTGRES_PASSWORD=your_secure_password_here
POSTGRES_HOST=db
POSTGRES_PORT=5432

# --- Debug Mode ---
# Set to False in production
DEBUG=True

# --- Allowed Hosts (comma-separated) ---
# Add your domain names here for production
ALLOWED_HOSTS=localhost,127.0.0.1,0.0.0.0,web

# --- API Settings ---
FPL_API_URL=https://fantasy.premierleague.com/api/bootstrap-static/

# --- Django Admin Settings ---
# Change this to a custom URL for security (e.g., secret-admin/)
ADMIN_URL=admin/
DJANGO_ADMIN_FORCE_ALLAUTH=False

# --- Security Settings ---
# Enable these in production with HTTPS
SECURE_SSL_REDIRECT=False
SECURE_HSTS_SECONDS=0
SECURE_HSTS_INCLUDE_SUBDOMAINS=False
SECURE_HSTS_PRELOAD=False
SECURE_CONTENT_TYPE_NOSNIFF=True
SECURE_BROWSER_XSS_FILTER=True
X_FRAME_OPTIONS=DENY

# --- Session Settings ---
# Enable secure cookies in production with HTTPS
SESSION_COOKIE_SECURE=False
SESSION_COOKIE_HTTPONLY=True
CSRF_COOKIE_SECURE=False
CSRF_COOKIE_HTTPONLY=True

# =============================================================================
# PRODUCTION NOTES:
# =============================================================================
# 1. Generate a new SECRET_KEY
# 2. Set DEBUG=False
# 3. Update ALLOWED_HOSTS with your domain
# 4. Use strong database passwords
# 5. Enable SSL/HTTPS security settings
# 6. Change ADMIN_URL to something unique
# =============================================================================